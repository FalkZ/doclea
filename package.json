{
  "name": "doclea-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm run build -r",
    "test": "pnpm run test -r",
    "dev": "pnpm run dev -r",
    "install": "pnpm install -r"
  }
}
